{
  "name": "@kjds/local-demo",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "engines": {
    "node": ">=24.0.0"
  },
  "scripts": {
    "build": "vite build && node --test tests/build/*.test.ts",
    "build:portable": "vite build && node scripts/build-portable.mjs",
    "dev": "vite --host 127.0.0.1 --port 43190",
    "preview": "vite preview --host 127.0.0.1 --port 43190 --strictPort",
    "test": "node --test tests/*.test.ts",
    "test:browser:offline": "vite build && node tests/browser/offline-shell.test.cjs",
    "test:delivery": "vite build && node --test tests/delivery/*.test.cjs",
    "test:portable": "vite build && node --test tests/portable/*.test.cjs",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "verify:delivery": "vite build && node scripts/verify-delivery.mjs"
  },
  "devDependencies": {
    "@types/node": "24.4.0",
    "playwright": "1.58.2",
    "typescript": "5.9.3",
    "vite": "8.2.0",
    "yauzl": "3.4.0",
    "yazl": "3.3.1"
  },
  "dependencies": {
    "@noble/hashes": "2.2.0"
  }
}
