# 决策与因果实验数值完整性验证

- 日期：2026-07-17
- Gate：G-1
- 结果：PASS
- 数据库：专用一次性 PostgreSQL `kjds_g1_smoke`
- Alembic head：`20260717_0032`
- 完整回归：124 passed
- G-1 开始：`2026-07-17T14:22:33.8416577Z`
- G-1 完成：`2026-07-17T14:24:15.9273576Z`
- 报告：`.runtime/G1_VERIFICATION.json`

## 验证范围

1. 决策合同最大损失只接受有限、非负 Decimal。
2. 分析置信度必须是有限的 `0..1`；预测值与上下界必须有限且区间有序。
3. 实际结果必须有限；数据库同时保护预测区间与实际结果。
4. 因果实验 MDE、预算和止损必须有限、为正，且止损不得超过预算。
5. 变体分配、效果乘数、实验观测和安全阈值必须有限。
6. `20260717_0032` 在 6 张表增加 7 条 PostgreSQL CHECK 约束。

## 绕过服务层验证

验证器先构造合法且最终清理的决策—实验链，再在回滚事务内隔离不可变账本触发器，直接攻击 CHECK 约束。PostgreSQL 拒绝：

- `NaN` 最大损失；
- `NaN` 置信度；
- 反向预测区间；
- `NaN` 实际结果；
- 高于预算的止损；
- `NaN` 实验观测；
- `NaN` 安全阈值。

验证数据在后续 API 烟测前按依赖顺序清理，避免污染预测校准和经营账本。

## 完整门禁

- Alembic 0032 升级、回退到 0024、再升级：PASS。
- Ruff：PASS。
- Pytest：124 passed；仅保留现有 Starlette/httpx 弃用警告。
- Next.js 生产构建：PASS。
- API health/auth、决策校准、因果实验、受控执行、恢复演练、Web 代理与清理：PASS。

## 未完成边界

- 该结果不证明因果策略、执行计划、能力经济账等所有 NUMERIC 字段都已完成同级审计。
- 真实 Ozon/银行数据、会计舍入和法定口径仍需一手文件与负责人批准。
- 0032 的备份恢复复演仍受当前环境缺少 `pg_dump`/`pg_restore` 与 Docker 控制权限阻塞；现有恢复证据停留在 0029。
