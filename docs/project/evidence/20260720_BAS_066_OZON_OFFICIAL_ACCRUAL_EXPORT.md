# BAS-066：Ozon 官方计提报表真实原件适配证据

| 元数据 | 值 |
|---|---|
| task_id | BAS-066 |
| requirement | BR-052 |
| status | DONE_ENGINEERING / NEEDS_INDEPENDENT_REVIEW |
| verified_at | 2026-07-20 |
| source surface | Ozon Seller → 财务 → 应计费用详细信息 |
| report period | 2025-10-01 至 2025-10-31 |
| original filename | `Отчет по начислениям_01.10.2025-31.10.2025.xlsx` |
| original byte size | 4,459 |
| SHA-256 | `489d4518e8e8c1f00c135cd1380ed636ff5e3ee1768182a9146b3cc4b1dcae68` |
| repository copy | 无；原件保留在本机下载目录，未提交 Git |

## 1. 取得方式与边界

在已登录的 Ozon Seller 会话中使用页面原生期间筛选选择 2025 年 10 月，再通过页面原生“下载报告”入口取得 XLSX。没有读取或保存平台密钥，没有调用写接口，也没有修改 Ozon 账户数据。

本次先完成原件下载、无状态预检、解析适配和内存暂存演练；随后在 2026-07-20 将完全相同字节正式写入本机 PostgreSQL Evidence/import 链。Evidence 为 `evd_902fe12a454e4703b88b6ad7314ed652`，import 为 `imp_76eab9701e954896a6f67ccdbb845cb6`。没有伪造第二个 Reviewer 身份，因此该原件仍未通过 BR-041 的独立来源复核，不能晋升正式事实或进入利润账。

## 2. 原件结构与真实语义

- 工作表：`Начисления`。
- 第一行：`Период: 01.10.2025-31.10.2025`。
- 第二行起为 16 列原始表头；核心列包括计提 ID、计提日期、服务组、计提类型、商家货号、SKU、商品名、数量、销售价、处理/服务日期、销售平台、工作模式、Ozon 费率、物流指标和卢布合计。
- 数据行：15。
- 服务组：6 种；计提类型：9 种。
- 两行总账调整没有官方计提 ID；适配器保留 payload 中的原始空值，并只在 normalized 层生成“文件哈希前缀 + 原始行号”的内部来源行 ID。

该文件同时包含正向销售收入、折扣积分、销售佣金、配送/国际配送、收单、合同冲抵和争议补偿。它不是纯费用代码表，因此不得被误识别为 `ozon_fee` 或把所有行映射为平台费用。

## 3. 工程增量

- 新增独立 `ozon_accrual` 合同；`ozon-v1` 的订单、费用、计提、退货和结算语义分离。
- XLSX 解析器只对明确的 `Период:` 单元格跳过标题行，不做任意行猜测。
- 只有金额表头明确写出 `Сумма итого, руб.` 时才派生 `RUB`。
- 修复通用规范化把数值 `0` 当空值的缺陷；真实文件中的 0 元行不再被错误拒绝。
- 日期单元格按 UTC 会计日边界进入 normalized 层；原始单元格值仍保留，系统不把该边界宣称为真实事件时刻。
- `ozon_accrual` 进入既有财务原件独立复核门，但显式拒绝直接生成 Finance Entry；会计分类必须另行批准。

## 4. 真实文件验证

无状态预检结果：

- `record_type=ozon_accrual`
- `row_count=15`
- `missing_columns=[]`
- `ready=true`
- SHA-256 与下载原件一致

隔离内存暂存结果：

- 状态 `completed`
- 接收 15 行、拒绝 0 行
- 两个缺失官方计提 ID 的来源行获得内部派生 ID
- 15 行 `amount` 精确求和为 `-9943.02 RUB`
- 与 Ozon 页面汇总显示的 `-9,943 RUB` 一致（页面按整数显示）

定向回归：23 passed；完整 Python：288 passed；Ruff 全库通过；G-1 真环境 PASS（迁移重放、数值约束、备份恢复、生产 API/Web/Worker 镜像、API/Web 健康、鉴权、Evidence、财务门、Kill Switch 与清理均通过）。机器结果见 `.runtime/G1_VERIFICATION.json`。

## 5. 仍未完成

1. 由非上传者确认真实账户导出、期间一致、非公开样例和完整导出。
2. 由不同身份 Reviewer 完成四项来源复核，使复核证明同时绑定现有原件和导入。
3. 获取月度代理报告/服务文件、结算文件、银行到账与实际换汇证据。
4. 由财务负责人逐项批准收入、折扣、佣金、物流、冲抵和补偿的会计分类与符号规则。
5. 完成订单—计提—结算—银行—FX 对账后，才可把实际成本写入 CM3。

因此 `OZN-002/FIN-001/UNK-006` 已从“缺真实计提原件”推进为“原件已正式存证和导入，但独立复核、会计分类和多方对账未完成”，仍保持 `PARTIAL_BLOCKED`。正式写入及零事实/零分录复验证据见 `20260720_BAS_068_OZON_ACCRUAL_FORMAL_PENDING_REVIEW.md`。
