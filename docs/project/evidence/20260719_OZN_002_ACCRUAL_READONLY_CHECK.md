# OZN-002 应计费用只读核验

| 项目 | 结果 |
|---|---|
| captured_at | 2026-07-19T21:34:21+08:00 |
| surface | Ozon Seller 应计费用详细信息 |
| mode | 已登录会话，只读 |
| external write | 无 |
| file downloaded | 是；2026-07-20 下载 2025 年 10 月官方计提 XLSX |
| formal evidence promoted | 否 |
| status | PARTIAL_BLOCKED（原件已取得，独立复核与对账未完成） |

## 原始报告下载与工程预检（2026-07-20）

- 已通过页面原生“下载报告”入口取得 `2025-10-01` 至 `2025-10-31` 官方计提 XLSX；大小 4,459 字节，SHA-256 为 `489d4518e8e8c1f00c135cd1380ed636ff5e3ee1768182a9146b3cc4b1dcae68`。
- 原件包含 15 条数据，6 个服务组和 9 个计提类型；逐行金额精确求和 `-9,943.02 RUB`，与页面整数汇总 `-9,943 ₽` 一致。
- 文件是计提总账而非纯费用表：包含销售收入、折扣积分、佣金、配送、国际配送、收单、合同冲抵和争议补偿。系统已新增 `ozon_accrual` 原样暂存合同，禁止将全表误作 `ozon_fee`。
- 无状态预检 `ready=true`；隔离内存暂存 15/15 行通过。原件没有提交 Git，也没有写入正式数据库。
- 详细工程和验证证据见 `20260720_BAS_066_OZON_OFFICIAL_ACCRUAL_EXPORT.md`。

## 历史交易月份定位（2026-07-19T23:34:00+08:00）

- 余额页使用原生期间筛选核验 `2025-10-01` 至 `2025-12-31`：销售 `15,658 ₽`、退货 `0 ₽`、Ozon 代理佣金 `−2,975 ₽`（`19.0%`）、其他应计项目 `−14,699 ₽`。
- `2025-11-01` 至 `2025-11-30` 和 `2025-12-01` 至 `2025-12-31` 均为销售 `0 ₽`、退货 `0 ₽`、代理佣金 `0 ₽`；因此把真实样本范围收敛到 2025 年 10 月。
- 余额页进一步核验 `2025-10-01` 至 `2025-10-31`：期间净应计 `−9,943 ₽`；销售 `15,658 ₽`；退货 `0 ₽`；代理佣金 `−2,975 ₽`；配送服务 `−313 ₽`；合作伙伴服务 `−7,753 ₽`；赔偿和赔偿返还 `139 ₽`；其他应计项目 `−14,699 ₽`；折扣积分 `2,634`（页面明确说明其当时不影响付款金额）。
- 上述值是 Ozon Seller 账户内的只读汇总，不是逐行应计费用、订单、结算或银行到账原件。`其他应计项目` 也不能按名称直接推定会计含义。
- 本次没有下载报告、接受条款、读取密钥或执行账户写操作。应计费用页确认存在“下载报告”入口，但下载文件属于新的本地写入动作，留待明确授权后执行。

## 补充核验（2026-07-19T22:08:13+08:00）

- 成功使用页面原生月份和年度筛选，没有再依赖猜测 URL。
- `2026-06-01` 至 `2026-06-30`、`2026-05-01` 至 `2026-05-31` 的应计费用明细均显示“未找到任何内容”。
- 财务余额页显示当前余额 `8,568 ₽`，七月月初余额也是 `8,568 ₽`；七月已应计和已支付均为 `0 ₽`。
- 余额页选择 `2026-01-01` 至 `2026-07-19` 后，期间内销售、退货和 Ozon 代理佣金均为 `0 ₽`。
- 由上述页面状态只能推断该余额在 2026 年以前已经形成；没有原始报告时，不能推断具体订单、费用组成、发生月份或会计归属。
- 本次仍未下载空报告、接受条款、读取密钥或执行账户写操作。

## 可确认事实

- 目标页面为 `https://seller.ozon.ru/app/finances/accruals?tab=ACCRUALS_DETAILS`，标题为 `Ozon: Торговая площадка`。
- 页面显示“应计费用详细信息”、期间筛选、应计项目类型和下载报告入口。
- 默认期间为 `2026-07-01` 至 `2026-07-19`。
- 页面在该默认期间显示“未找到任何内容”，因此没有把空页面包装成费用样本，也没有触发报告下载。
- 页面提示 2026-08-03 后旧商品报告将完全由应计费用报告取代。

## 未确认

- 2025 年 10 月订单、结算、月度代理/服务文件与退货报告的原始内容；计提报表已经取得。
- 计提类型到正式会计科目的批准映射、结算周期以及银行/FX 口径。
- 下载文件是否带平台可验证的任务 ID、签名或其他来源证明。

## 边界与下一步

当前已拥有原始下载文件、SHA-256 和通过的工程预检，但仍没有独立复核、正式 Evidence/import ID、逐类会计分类、月度代理/服务文件、结算、银行和 FX 证据。因此仍不能进入利润账或完成对账。下一步是由独立 Reviewer 复核并正式存证，再按“逐类会计分类 → 正式事实晋升 → Ozon/结算/银行/FX 对账”处理。
