# BAS-047 三候选离线证据包

| 字段 | 值 |
|---|---|
| 日期 | 2026-07-19 |
| Gate | G0 |
| 状态 | DONE_ENGINEERING |
| 合同 | `kjds-startup-package-v4` |
| 业务事实晋升 | 否 |
| 平台写入 | 否 |

## 交付

- 新增 `web/public/startup/candidate-research.csv`，固定三个候选、每候选五类指标的空白收集结构。
- 启动资料包从七份升级为八份；私密副本只补缺失模板，不覆盖经营人员已填写内容。
- 校验候选数量、固定指标覆盖、重复行、RU/OZON 边界、观察窗口、样本量、值域、可信度、带时区观察时间和至少两个来源族。
- 测量窗口与样本下限通过自动测试和 `MarketIntelligenceService.CANDIDATE_MEASUREMENT_CONTRACTS` 对齐，防止离线模板与正式预检漂移。
- Web 的 SKU-001 启动步骤先下载候选研究清单，并把 Passport 清单作为第二模板。

## 安全边界

- 公开模板不含真实商品、供应商、账户或密钥。
- 校验器不读取 `evidence_reference` 指向的内容，不上传文件，不写数据库。
- `ready_for_human_intake` 只表示资料字段可交人工录入，不表示证据有效、候选通过、Gate 放行或可以采购/上架。
- 正式事实仍必须经过 Evidence Blob 固化、哈希复验、五指标原子预检、人工报价工作区交接和三报价服务端门禁。

## 验证

- 启动资料包专项：6 项通过。
- 完整 Python 回归：220 项通过；Ruff 通过。
- Web 契约/身份安全测试：7 项通过；生产构建通过。
- 密钥扫描：296 个非忽略文件通过。
- 公开空模板结构有效；严格模式按设计以退出码 3 等待真实输入。
- 完整 G-1 于 2026-07-19 PASS；隔离恢复 SHA-256 为 `82e678ce3fc8a42f6a2f227f1e27511782a586684d742b20f72fdcf969deee93`，运行摘要以 `.runtime/G1_VERIFICATION.json` 为准。

## 未解除项

- 尚未提供三个真实候选及其原件，`SKU-001/UNK-001` 继续阻塞。
- 50/50/30 仍是待经营负责人复核的工程默认值，`UNK-014` 未解除。
- 本交付不创建 Product、Passport、供应商报价、采购申请、Listing 或 Ozon 写操作。
